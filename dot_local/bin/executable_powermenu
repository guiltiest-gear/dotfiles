#!/usr/bin/env bash

set -eu

case "$(printf "Cancel\nShutdown\nRestart\nLock\n" | dmenu -l 4)" in
  "Cancel")
    exit
    ;;
  "Shutdown")
    shutdown now
    ;;
  "Restart")
    reboot
    ;;
  "Lock")
    slock
    ;;
esac
